import { View, Text, Image } from "react-native";
import React from "react";

const ChowLogo = () => {
	return (
		// <View>
		<Image source={require("../assets/images/chow-logo.jpeg")} height={10} width={10} />
		// </View>
	);
};

export default ChowLogo;
